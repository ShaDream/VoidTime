namespace VoidTime.GUI
{
    public class TextRenderer
    {
        public QuadData[] GetTextQuads(string text, FontAtlas atlas)
        {
            var data = new QuadData[text.Length];

            for (var i = 0; i < text.Length; i++)
            {
                var symbol = text[i];


            }
        }
    }
}